# Claude Code Pointer

Read `AGENTS.md` and treat it as the canonical repository-wide instructions.

# Telegator fork

Before anything else, read `TELEGATOR.md`: fork-specific rules that take precedence over `AGENTS.md`.
